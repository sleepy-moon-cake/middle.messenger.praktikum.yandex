export enum Errors {
  EVENT_IS_NOT_REGISTERED = "The event is not registered",
  TAG_NAME_IS_NOT_CORRECT = "The tag name is not correct",
  WRAPP_IS_NOT_CORRECT = "THe component must contain one root tag",
}
